//setInterval

console.log("ABC");
setInterval(() => {
    console.log("Hello", "Hello");
}, 5000);

console.log("Hllo Hllo");


/* The difference between setTimeout and setInterval is that:
setTimeout run the code only once .

but, setinterval keeps running the code in that interval again and again untill it is stopped.

*/