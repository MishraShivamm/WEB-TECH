//Qs3.Create a new array whose elements are in uppercase of words present in the original array.

let array = ["abcd","efgh"];
let newArray = array.map(words=> words.toUpperCase());
console.log(newArray);